"""Phase 4: career-copilot features built on the matching layer.

Each feature follows the same shape: a deterministic core computed
from the corpus + profile (always works, always testable), and an
optional LLM pass that only rephrases that core into prose (llm.py).
"""
